# tests/_known-failures.txt — read by run-all.sh
#
# One test name per line (no `.sh`, no `tests/` prefix). Lines starting with
# `#` are comments. Blank lines ignored.
#
# A test listed here still RUNS, but failure is marked KNOWN_FAILURE (yellow)
# instead of FAIL (red). The suite exit code stays 0 if every non-listed test
# passes.
#
# Policy: lines must include a rationale comment naming the sprint that will
# graduate the test. Adding a test here without a sprint pointer is a
# "policy-without-test" Sign violation. When a test is fixed, REMOVE the line
# (and its rationale). When the list is empty, delete this file.
#
# Created: sprint-v13-01 Phase D — to unblock CI without papering over real bugs.
# Graduation target: sprint-v13-01 Phase B (8pt test-coverage sprint).

# GRADUATED 2026-05-07 in sprint-v13-01-phase-b-chunk1 — see commit history.
# Audit was wrong: test had 11 cases, 2 failing, not "2 assertions surface-only."
# Real bug: `$(dirname "$0")/aegis-design-lint.sh` resolved to tests/ but the
# lint script is in tools/. Silent FAIL on every "valid DESIGN.md" case
# (TC-2, TC-7). Fixed by using ${_SCRIPT_DIR}/../tools/. Test now 11/11 PASS.
# (No longer in this list — entry kept here as graduation provenance.)
# aegis-block0-f-gate-test

# v11-specific install delivery check; test predates v12 graph + doc-canon
# additions and now fails because expected v11 file inventory has grown.
# Phase B story B2: rewrite to enumerate from a manifest, not hardcode.
aegis-install-v11-delivery-test

# GRADUATED 2026-05-07 in sprint-v13-01-phase-b-chunk2 — see commit history.
# Audit was wrong: not "brain-state-dependent". Real bug was a silent-exit in
# run_test() — `[[ $VERBOSE -eq 1 ]] && echo ...` returned non-zero when
# VERBOSE=0, and `set -e` (enabled mid-test on lines 114/130/165/204) killed
# the script after run_test 3. Fixed with explicit `return 0` in run_test.
# Test now 10/10 pass with no fixture changes needed.
# (No longer in this list — entry kept here as graduation provenance.)
# aegis-instinct-promote-test

# PARTIAL FIX 2026-05-07 in sprint-v13-01-phase-b-chunk2.
# What landed in chunk-2: (a) trace-audit teaches itself `_archived/`+`tests/`
# fallback recognition, (b) SI.02 path drift cleaned (tinman-heartbeat removed,
# 7 rows updated to disk truth). Test passes on macOS local + macOS-latest CI.
# Still failing on ubuntu-latest CI (T1+T2 — audit exits non-zero on clean
# state). Suspected cause: Check 4 FUNC-catalog drift detection sees Linux vs
# macOS sort/find ordering differences, OR Check 5 doc-registry hits a
# python3-version-specific quirk on the Ubuntu runner. Reproduces only in
# CI — needs Docker repro to root-cause.
# Phase B chunk-3 target: add `LC_ALL=C` to func-catalog sort/find OR teach
# audit to be platform-agnostic. Then graduate.
aegis-trace-audit-test
